Users can delete their data by signing out of the app, which clears all locally stored information including authentication tokens and preferences.

- Signing out clears all local data (tokens, preferences, watch history)
- Users can disconnect linked services (Trakt, debrid) which removes stored credentials
- Firebase Crashlytics data is anonymized/aggregated
